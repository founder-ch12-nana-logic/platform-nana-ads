# platform-nana-ads Consent Policy

- Consent is required before any tracking is enabled.
- Providers default to `tracking_enabled: false` and `requires_consent: true`.
- Events with `contains_pii: true` remain blocked until consent and privacy review are complete.
- No production tracking is permitted without an approved consent policy and checklist completion.
